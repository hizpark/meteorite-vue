<!--
  @file App.vue
  @description Vue 根组件，作为应用顶层组件，主要功能：
  1. 提供 <router-view /> 占位，用于渲染当前路由对应的组件
  2. 根组件本身不包含业务逻辑，所有页面逻辑放在各子组件中
  3. 样式由 main.js 全局引入 Element Plus 和全局样式
-->

<template>
  <!-- 路由视图占位，渲染当前路由页面 -------------------------------------- -->
  <router-view />
</template>

<script>
export default {
  // 根组件名称 ---------------------------------------------------------------
  name: 'App',

  // 根组件逻辑 ---------------------------------------------------------------
  // 本组件不含具体业务逻辑，主要用于路由渲染
  // 各页面或子组件负责具体业务功能
}
</script>

<!-- 样式部分 ----------------------------------------------------------------
     根组件未定义样式，全局样式由 main.js 引入
-->
